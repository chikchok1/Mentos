spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mentos?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: "여기에_MySQL_비밀번호"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: "여기에_32글자_이상의_시크릿키_입력"
    access-expiration-ms: 3600000
    refresh-expiration-ms: 1209600000
  oauth:
    google:
      client-id: "여기에_구글_클라이언트_ID"
    kakao:
      rest-api-key: "여기에_카카오_REST_API_키"
    gemini:
      api-key: "여기에_Gemini_API_키"
